# Development (localhost)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/scriptcheck?schema=public"
JWT_SECRET="change_me_in_production"
PORT=3001
APP_ENV=development

# Beta (set in Render / hosting — do not use production secrets here)
# APP_ENV=beta
# UPLOAD_DIR=/opt/render/project/src/backend/uploads
# CORS_ORIGINS=https://beta.scriptcheck.co.za

# Production (scriptcheck.co.za — separate deployment, separate database)
# APP_ENV=production
# DATABASE_URL=...
# JWT_SECRET=...
